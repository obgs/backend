type Favorites {
  total: Int!
  users: [User!]!
}

extend type Game {
  favorites: Favorites!
}

extend type Mutation {
  addOrRemoveGameFromFavorites(gameId: ID!, favorite: Boolean!): Boolean!
    @authenticated
}
