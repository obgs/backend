// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent/dialect/sql"
	"github.com/open-boardgame-stats/backend/internal/ent/game"
	"github.com/open-boardgame-stats/backend/internal/ent/match"
	"github.com/open-boardgame-stats/backend/internal/ent/schema/guidgql"
)

// Match is the model entity for the Match schema.
type Match struct {
	config
	// ID of the ent.
	ID guidgql.GUID `json:"id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the MatchQuery when eager-loading is set.
	Edges        MatchEdges `json:"edges"`
	game_matches *guidgql.GUID
}

// MatchEdges holds the relations/edges for other nodes in the graph.
type MatchEdges struct {
	// Game holds the value of the game edge.
	Game *Game `json:"game,omitempty"`
	// Players holds the value of the players edge.
	Players []*Player `json:"players,omitempty"`
	// NumericalStats holds the value of the numerical_stats edge.
	NumericalStats []*NumericalStat `json:"numerical_stats,omitempty"`
	// EnumStats holds the value of the enum_stats edge.
	EnumStats []*EnumStat `json:"enum_stats,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [4]bool
	// totalCount holds the count of the edges above.
	totalCount [4]map[string]int

	namedPlayers        map[string][]*Player
	namedNumericalStats map[string][]*NumericalStat
	namedEnumStats      map[string][]*EnumStat
}

// GameOrErr returns the Game value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e MatchEdges) GameOrErr() (*Game, error) {
	if e.loadedTypes[0] {
		if e.Game == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: game.Label}
		}
		return e.Game, nil
	}
	return nil, &NotLoadedError{edge: "game"}
}

// PlayersOrErr returns the Players value or an error if the edge
// was not loaded in eager-loading.
func (e MatchEdges) PlayersOrErr() ([]*Player, error) {
	if e.loadedTypes[1] {
		return e.Players, nil
	}
	return nil, &NotLoadedError{edge: "players"}
}

// NumericalStatsOrErr returns the NumericalStats value or an error if the edge
// was not loaded in eager-loading.
func (e MatchEdges) NumericalStatsOrErr() ([]*NumericalStat, error) {
	if e.loadedTypes[2] {
		return e.NumericalStats, nil
	}
	return nil, &NotLoadedError{edge: "numerical_stats"}
}

// EnumStatsOrErr returns the EnumStats value or an error if the edge
// was not loaded in eager-loading.
func (e MatchEdges) EnumStatsOrErr() ([]*EnumStat, error) {
	if e.loadedTypes[3] {
		return e.EnumStats, nil
	}
	return nil, &NotLoadedError{edge: "enum_stats"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Match) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case match.FieldID:
			values[i] = new(guidgql.GUID)
		case match.ForeignKeys[0]: // game_matches
			values[i] = &sql.NullScanner{S: new(guidgql.GUID)}
		default:
			return nil, fmt.Errorf("unexpected column %q for type Match", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Match fields.
func (m *Match) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case match.FieldID:
			if value, ok := values[i].(*guidgql.GUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				m.ID = *value
			}
		case match.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field game_matches", values[i])
			} else if value.Valid {
				m.game_matches = new(guidgql.GUID)
				*m.game_matches = *value.S.(*guidgql.GUID)
			}
		}
	}
	return nil
}

// QueryGame queries the "game" edge of the Match entity.
func (m *Match) QueryGame() *GameQuery {
	return (&MatchClient{config: m.config}).QueryGame(m)
}

// QueryPlayers queries the "players" edge of the Match entity.
func (m *Match) QueryPlayers() *PlayerQuery {
	return (&MatchClient{config: m.config}).QueryPlayers(m)
}

// QueryNumericalStats queries the "numerical_stats" edge of the Match entity.
func (m *Match) QueryNumericalStats() *NumericalStatQuery {
	return (&MatchClient{config: m.config}).QueryNumericalStats(m)
}

// QueryEnumStats queries the "enum_stats" edge of the Match entity.
func (m *Match) QueryEnumStats() *EnumStatQuery {
	return (&MatchClient{config: m.config}).QueryEnumStats(m)
}

// Update returns a builder for updating this Match.
// Note that you need to call Match.Unwrap() before calling this method if this Match
// was returned from a transaction, and the transaction was committed or rolled back.
func (m *Match) Update() *MatchUpdateOne {
	return (&MatchClient{config: m.config}).UpdateOne(m)
}

// Unwrap unwraps the Match entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (m *Match) Unwrap() *Match {
	_tx, ok := m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Match is not a transactional entity")
	}
	m.config.driver = _tx.drv
	return m
}

// String implements the fmt.Stringer.
func (m *Match) String() string {
	var builder strings.Builder
	builder.WriteString("Match(")
	builder.WriteString(fmt.Sprintf("id=%v", m.ID))
	builder.WriteByte(')')
	return builder.String()
}

// NamedPlayers returns the Players named value or an error if the edge was not
// loaded in eager-loading with this name.
func (m *Match) NamedPlayers(name string) ([]*Player, error) {
	if m.Edges.namedPlayers == nil {
		return nil, &NotLoadedError{edge: name}
	}
	nodes, ok := m.Edges.namedPlayers[name]
	if !ok {
		return nil, &NotLoadedError{edge: name}
	}
	return nodes, nil
}

func (m *Match) appendNamedPlayers(name string, edges ...*Player) {
	if m.Edges.namedPlayers == nil {
		m.Edges.namedPlayers = make(map[string][]*Player)
	}
	if len(edges) == 0 {
		m.Edges.namedPlayers[name] = []*Player{}
	} else {
		m.Edges.namedPlayers[name] = append(m.Edges.namedPlayers[name], edges...)
	}
}

// NamedNumericalStats returns the NumericalStats named value or an error if the edge was not
// loaded in eager-loading with this name.
func (m *Match) NamedNumericalStats(name string) ([]*NumericalStat, error) {
	if m.Edges.namedNumericalStats == nil {
		return nil, &NotLoadedError{edge: name}
	}
	nodes, ok := m.Edges.namedNumericalStats[name]
	if !ok {
		return nil, &NotLoadedError{edge: name}
	}
	return nodes, nil
}

func (m *Match) appendNamedNumericalStats(name string, edges ...*NumericalStat) {
	if m.Edges.namedNumericalStats == nil {
		m.Edges.namedNumericalStats = make(map[string][]*NumericalStat)
	}
	if len(edges) == 0 {
		m.Edges.namedNumericalStats[name] = []*NumericalStat{}
	} else {
		m.Edges.namedNumericalStats[name] = append(m.Edges.namedNumericalStats[name], edges...)
	}
}

// NamedEnumStats returns the EnumStats named value or an error if the edge was not
// loaded in eager-loading with this name.
func (m *Match) NamedEnumStats(name string) ([]*EnumStat, error) {
	if m.Edges.namedEnumStats == nil {
		return nil, &NotLoadedError{edge: name}
	}
	nodes, ok := m.Edges.namedEnumStats[name]
	if !ok {
		return nil, &NotLoadedError{edge: name}
	}
	return nodes, nil
}

func (m *Match) appendNamedEnumStats(name string, edges ...*EnumStat) {
	if m.Edges.namedEnumStats == nil {
		m.Edges.namedEnumStats = make(map[string][]*EnumStat)
	}
	if len(edges) == 0 {
		m.Edges.namedEnumStats[name] = []*EnumStat{}
	} else {
		m.Edges.namedEnumStats[name] = append(m.Edges.namedEnumStats[name], edges...)
	}
}

// Matches is a parsable slice of Match.
type Matches []*Match

func (m Matches) config(cfg config) {
	for _i := range m {
		m[_i].config = cfg
	}
}
